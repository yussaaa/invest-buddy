global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "backend"
    static_configs:
      - targets: ["host.docker.internal:8000"]  # backend runs on host, not in Docker
    metrics_path: /metrics
